{
  "name": "minesweeper",
  "homepage": "https://cscanlin.github.io/minesweeper",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-scripts": "0.8.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "standard": "^11.0.1"
  }
}
